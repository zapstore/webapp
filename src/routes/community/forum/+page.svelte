<script lang="js">
	/**
	 * /community/forum — feed UI is `CommunityForumShell` in `+layout.svelte` (keep-alive vs Activity).
	 * This route exists for URL, `+page.js` seed, focus/SEO, and meta (canonical/OG).
	 */
	import SeoHead from '$lib/components/layout/SeoHead.svelte';
</script>

<SeoHead title="Forum — Zapstore" />
