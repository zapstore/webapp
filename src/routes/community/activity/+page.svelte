<script lang="js">
	/**
	 * /community/activity — feed UI is `CommunityActivityShell` in `+layout.svelte` (keep-alive vs Forum).
	 * Meta (canonical/OG) via SeoHead; shell provides the feed.
	 */
	import SeoHead from '$lib/components/layout/SeoHead.svelte';
</script>

<SeoHead title="Activity — Zapstore" />
